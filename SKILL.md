# 六路采集 Skill

> 输入一个人名/主题名，6 路并行采集信息，父进程汇总后直接呈现。

---

## 安装后：第一步配置指引

> 下载安装后，请先完成以下两步配置才能正常使用。

### 配置 1：搜索 API（Tavily）

搜索核心依赖 `web_search` 工具。Tavily 是默认搜索提供方，需要在 OpenClaw 中配置 API Key：

**方式一（推荐）**：在 `openclaw.yml` 的 `plugins` 段添加：

```yaml
plugins:
  tavily:
    apiKey: "你的Tavily API Key"
```

**方式二**：在 `openclaw.yml` 的 `webSearch` 段添加：

```yaml
webSearch:
  provider: tavily
  apiKey: "你的Tavily API Key"
```

> ⚠️ 注意：两种配置位置的 Key 格式不同，如果一种方式报 422，尝试另一种。
> 获取 Tavily Key：https://tavily.com/#api

### 配置 2：Chrome 调试端口（用于登录平台采集）

小红书、微博等平台需要登录才能访问。通过 Chrome 远程调试端口让 OpenClaw 浏览器借用已登录的 Chrome：

```
1. 完全关闭所有 Chrome 窗口
2. 按 Win+R，输入以下命令后回车：
   "C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=18800 --remote-allow-origins=*
3. Chrome 启动后，登录小红书/微博等需要采集的平台
4. 登录完成后通知 Agent
```

> ⚠️ 如果端口不监听：Chrome 148+ 需加 `--user-data-dir` 参数
> `"C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=18800 --remote-allow-origins=* --user-data-dir="C:\Users\你的用户名\AppData\Local\Google\Chrome\User Data"`

### 下一步

完成以上两步配置后，就可以开始采集了。查看下方「使用示例」了解具体用法。

---

## 一句话用法

```
> 六路采集 Elon Musk
> 六路采集 特朗普
```

## 核心设计原则

1. **子 Agent 返回文本，不写文件**：子 Agent 超时后文件不会持久化。改为通过 `sessions_spawn` 返回文本结果，父进程收集后汇总展示。
2. **搜索失败必须有 fallback**：`web_search`（Tavily）可能限流/过期/配置错误，必须兜底到 `web_fetch` + 搜索引擎直抓。
3. **浏览器可弥补无法搜索的内容**：知乎公开文章可用 OpenClaw 浏览器直接读；小红书/B站等需登录的平台，引导用户开启 Chrome 调试端口后通过 `target="host"` 访问。
4. **子 Agent 任务要极度聚焦**：每个子 Agent 只做一个维度的搜索 + 内容获取，避免 15-20 分钟超时被杀。

---

## 完整工作流程

### Step 1：确认采集目标

问用户要采集什么。默认 6 路全跑，也支持用户指定只跑某几路。

### Step 2：确定搜索策略

**搜索优先级链：**
```
Tavily web_search（主搜）
  → 失败时: web_fetch 直抓 Google/Bing 搜索结果
    → 失败时: 启动 OpenClaw 浏览器导航到搜索引擎
      → 登录墙: 引导用户开启 Chrome 调试端口登录后读取
```

**平台可访问性对照表（供 Agent 参考）：**

| 平台 | OpenClaw 浏览器 | 需要用户登录 |
|------|:--------------:|:----------:|
| 知乎（公开文章） | ✅ 可直接读 | ❌ |
| 网易/搜狐/163 | ✅ web_fetch 可读 | ❌ |
| 百度/Google/Bing | ✅ browser 可读 | ❌ |
| 小宇宙播客 | ⚠️ 部分可读 | 有时需要 |
| B站 | ✅ 搜索+视频可见 | ❌ |
| 小红书 | ❌ 搜索/主页均需登录 | ✅ 必须 |
| 微信公众号 | ❌ 需要登录和转发 | ✅ |

### Step 3：6 路并行采集（核心）

同时 spawn 6 个 sub-agent，每个独立完成调研。一次性全部 spawn 然后 `sessions_yield()`。

```
sessions = []
foreach agent in 6:
    task = "你是[维度名]调研专家。目标：[目标名]。任务：调研[维度]\n\n..."
    sessions_spawn(task=task, runtime="subagent", mode="run")
    sessions.append(sessionKey)

sessions_yield()
```

#### 各 Agent 的维度定义

##### Agent 1：著作/论文
- 搜索：学术出版、研究报告、专栏文章
- 输出：标题+年份、核心主张、关键论点（3-5）、引用/影响力
- 边界：如果目标不属于学术型，标注「非学术型产出者，核心产出是 [具体形式]」

##### Agent 2：播客/访谈
- 搜索：播客节目、视频访谈、公开演讲
- 输出：来源+时长+日期、核心观点、语气风格
- 要点：记录重复出现的观点（一致性信号）和矛盾观点（演化信号）

##### Agent 3：社交媒体
- 搜索：小红书、B站、知乎、抖音、微博等
- 输出：各平台账号信息、高频主题、代表作品、互动量级
- **只采集高互动内容**：跳过播放/点赞低的普通内容，专注爆款（500+赞/高播放）
- **爆款笔记分析框架**：每篇分析核心观点/独白金句/自我评价/转化路径/用户互动/争议点
- **IP定位综合分析**：采集完成后，综合所有笔记的模式分析：内容演变、标签策略、变现路径、叙事风格一致性、定位变迁
- **所有数据附来源链接**：每条信息后面跟一个可点击的 URL
- **注意平台活跃度**：如果某平台内容极少或互动极低，在报告中标注「该平台非目标主阵地」
- 注意：小红书搜索需要登录 → 引导用户启动 Chrome 调试端口

##### Agent 4：批评者视角
- 搜索：争议、质疑、反驳文章
- 输出：主要批评点、批评者背景、目标回应
- 原则：客观呈现，不强行制造争议

##### Agent 5：决策/成就记录
- 搜索：重大事件、关键决策、荣誉奖项
- 输出：时间+背景、决策内容、结果、事后评价
- 重点：商业里程碑、项目状态、融资/合作情况

##### Agent 6：时间线
- 搜索：全量信息的时间排序
- 输出：表格（时间｜事件｜影响），标注关键转折点
- 要点：观点的演化轨迹（早期 vs 后期）

#### Agent Prompt 模板

```
系统角色：你是 [维度名] 调研专家。
目标：调研 [目标名] 的 [维度]。

搜索策略：
1. 优先使用 web_search 搜索中英文信息
2. web_search 失败时尝试 web_fetch 直接抓取 Google 搜索结果
3. 对找到的链接用 web_fetch 深入阅读全文
4. 中文平台（百度/Google等）如被拦，改用浏览器 snapshot 读取

输出要求：
1. 每条信息标注来源 URL
2. 信息不足时明确标注"该维度公开信息有限"
3. 标注信息来源的可靠性（一手/二手/推测）
4. 用 markdown 格式返回完整调研结果
5. 如果搜索 API 不可用，先执行 web_fetch 爬取搜索引擎的原始结果页

约束：
- 不要编造信息，宁缺毋滥
- 不要写文件到本地
- 把调研结果作为这个 agent 的最终回复返回
```

---

### Step 4：父进程汇总

6 个子 Agent 返回后，父进程收集所有文本结果，生成一份汇总摘要呈给用户。

**汇总模板：**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  [目标名] · 六路采集完成
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 著作（Agent 1）
  [完整调研结果，包含所有原始细节]

🎙️ 播客/访谈（Agent 2）
  [完整调研结果]

📱 社交媒体（Agent 3）
  [完整调研结果，包含每篇爆款的来源链接]

  **IP定位综合分析**：
  - 内容形式演变：xxx
  - 标签策略：xxx
  - 变现路径：xxx
  - 叙事风格一致性：xxx
  - 定位变迁：xxx

⚡ 批评者视角（Agent 4）
  [完整调研结果]

📋 决策记录（Agent 5）
  [完整调研结果]

📅 时间线（Agent 6）
  [完整调研结果]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

想要深入某个维度，或对某条信息进一步追问？
```

---

## 浏览器登录模式（针对需要登录的平台）

### 需要用户操作

当搜索小红书等需登录的网站时，引导用户：

```
步骤如下：
1. 关闭所有 Chrome 窗口
2. 按 Win+R，输入以下命令后回车：
   "C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=18800 --remote-allow-origins=*
3. Chrome 启动后，登录小红书/B站/知乎
4. 登录好后通知我
```

> ⚠️ 注意：Chrome 148+ 版本需要同时指定 `--user-data-dir` 参数才能生效，如果端口不监听，请加上 `--user-data-dir=C:\Users\FD\AppData\Local\Google\Chrome\User Data`。

### 浏览器登录后的操作模式

用户登录后，可以直接使用 `target="host"` 的 browser 工具操作：
- 导航到目标 URL 进行搜索
- snapshot 读取页面内容
- 每次切换页面用 navigate，不要重复 open 新标签
- **小红书笔记 URL 格式**：`/explore/{noteId}?xsec_token=...` — 从 profile 页面的链接中提取 noteId 和 xsec_token，拼接到 explore 路径下即可

> 注意：默认的 `target="host"` 不带 profile 参数，此时使用的是 OpenClaw 管理的浏览器实例，不是用户的 Chrome。浏览器工具会通过 MCP 自动发现用户已在调试端口运行的 Chrome 实例。如果 profile="user" 失败，直接用 target="host" 即可。

### 视频号数据采集（友望数据）

友望数据（www.youwant.cn）是专业的视频号数据分析平台，支持搜索视频、导出数据。

#### 标准操作流程

```
1. 打开 Chrome 调试端口 → 登录友望数据（需已注册付费账号）
2. 浏览器 navigate 到 https://www.youwant.cn
3. 点击「进入友望」进入后台
4. 左侧导航 → 视频数据 → 视频搜索
5. 输入关键词搜索
6. 设置时间范围（近60天/近90天等）
7. 点击「导出」按钮 → 弹出确认框 → 点击「导 出」
8. 导出完成后，点击「数据导出管理」→「导出历史」查看已生成的文件
```

#### 导出文件格式

- 文件格式为 `.xls`（OLE2 格式），可用 Excel 打开
- 每次导出消耗 1 次导出次数（账号有月配额）
- 导出历史中状态「已完成」后可下载

#### 下载文件的方法

**方法一（用户手动下载）**：在导出历史页面，手动点击「下载」按钮 → 文件保存到浏览器默认下载路径。这是最稳定的方式，因为用户手动操作不会被平台反自动化机制拦截。

**方法二（拦截网络请求获取下载URL）**：
```javascript
// 在页面中注入拦截代码，点击下载后捕获请求
// 触发 download 后会先 POST 到 API 获取真实文件 URL
// POST: //api.youwant.cn/v1/TaskCenter/DownloadExcel?autoId=<id>
// 然后跳转到文件地址: //res.youwant.cn/export/<dir>/<hash>
// 导航浏览器到该 URL 可触发下载
```

#### 已知问题

| 问题 | 说明 | 处理方式 |
|------|------|----------|
| CDP 下载被拦截 | 友望数据检测到 Chrome DevTools 自动化连接，下载的文件返回伪造数据 | 让用户手动点击下载按钮 |
| 文件看似损坏 | 实际 XLS 格式正确（OLE2），但内容可能被平台加密/替换 | 手动下载可解决 |
| 导出次数有限 | 每个账号每月有导出次数上限 | 合理规划导出计划 |
| 结果上限500条 | 单次导出最多导出前500条 | 如需更多，分批按条件导出 |

#### 核心经验

- **友望数据不支持 CDP 自动下载**：平台会检测 `navigator.webdriver` 标记，被 CDP 控制的浏览器会被限制真实数据下载。Override `navigator.webdriver` 无法生效（不可重定义）。
- **手动下载是唯一可靠方式**：让用户在 Chrome 浏览器中自己点下载，不经过 CDP 操作。
- **网络拦截可获取文件URL**：通过注入 fetch/XHR 拦截可以拿到文件下载地址，但同样触发反自动化保护。

---

## 错误处理

| 场景 | 处理方式 |
|------|----------|
| web_search 返回 422/Tokens 不足 | 检查配置：Tavily key 可能在 `webSearch` 或 `tavily` 插件配置中，两者格式不同；尝试用 web_fetch 替代 |
| sub-agent 超时（>15min） | 记录已收集到的信息，标注"部分完成"，父进程不等待该 agent |
| 信息源极少（<3 个） | 在报告中标注「该维度公开信息有限」 |
| Agent 间结论矛盾 | 在汇总中并列呈现，不强行调和 |
| 浏览器 snapshot 不全 | 用 screenshot 补足图片内容，或多次 snapshot 滚动 |
| web_fetch 返回验证码/阻断 | 改用浏览器工具 navigate 到目标 URL 再 snapshot |
| Chrome 调试端口不监听 | Chrome 148+ 需加 `--user-data-dir` 参数；检查任务管理器是否有残留 Chrome 进程 |

## 与 company-skill（女娲.skill）的关系

此 SKILL.md 为「六路精品采集」功能的独立实现，包含以下优化：
- 去掉了原公司 skill 中的写文件逻辑（超时不持久）
- 加入了搜索 fallback 策略（Tavily 不可用时的兜底方案）
- 加入了浏览器登录模式（小红书等需登录的平台）
- 集成本次测试发现的 Chrome 148 兼容性问题修复

---

## 测试复盘（2026-05-19）

### 测试目标：潘妙言（深圳大学/非遗文化）

### 各 Agent 实际表现

| Agent | 数据量 | 主要来源 | 耗时 |
|-------|:-----:|----------|:----:|
| 1 著作 | 少量 | 搜索摘要 | 快速 |
| 2 播客 | 3个来源 | 知乎专栏全文（√）, 小宇宙（仅摘要）, B站（url） | 中等 |
| 3 社交媒体 | 丰富 | 小红书主页+笔记标题+互动数据 | 需登录 |
| 4 批评者 | 1条 | 福布斯FGA性质辨析 | 快速 |
| 5 决策 | 丰富 | 深大官网+网易新闻+搜索 | 中等 |
| 6 时间线 | 完整 | 知乎访谈+网易文章+深大官网+搜索 | 中等 |

### 小红书内容采集策略（本次实测关键发现）

小红书主页**分页加载**，默认只显示前10-12条笔记。需要**滚动**才能加载更多：

```
// 使用 CDP 或 browser 工具多次滚动加载
window.scrollTo(0, document.body.scrollHeight);
// 每滚动一次加载约10条，建议滚动3-5次
```

**笔记内容提取**：
- 滚动加载后，snapshot 可获取所有笔记的**标题 + 互动数据（赞/藏）**
- 要获取**笔记正文**，需逐个 navigate 到笔记详情页再 snapshot
- **URL 结构**：profile 中的链接格式为 `/user/profile/{userId}/{noteId}?xsec_token=...`，完整详情的链接格式为 `/explore/{noteId}?xsec_token=...`，xsec_token 通用
- **只采集高互动笔记**：优先读置顶笔记和点赞 >500 的高互动笔记，低互动笔记不做深度分析
- 笔记正文可直接从 snapshot 中提取文字（小红书 web 版渲染为纯文本）
- **每条数据必须附来源链接**，在文档中内联插入

**爆款笔记的分析维度**：

每篇高互动笔记（爆款）应分析以下 6 个维度：

| 维度 | 说明 |
|------|------|
| **核心观点** | 笔记传递的主要信息/价值观 |
| **独白金句** | 评论区/正文中的金句 |
| **自我评价** | 从创作者评论或正文中提炼的自我定位 |
| **转化路径** | 变现引导方式（评论→进群→付费） |
| **用户互动** | 最高赞评论内容（反映受众反应） |
| **争议点** | 负面/批评性评论（反映内容争议性） |

**IP定位综合分析**：完成全部笔记采集后，进行的综合分析：
- 内容形式演变（早期图文 → 后期短剧）
- 标签策略（重复使用的核心标签）
- 变现路径（流量→社群→付费的完整链路）
- 叙事风格一致性（重复出现的自我定位）
- 定位变迁（最初的定位 vs 现在的定位）

**本次实测数据**（妙言（高能人生）✨ 小红书）：
- 共获取 **33 条笔记**标题 + 互动数据
- 点赞范围：32 ~ 1.5万
- 时间跨度：2024年3月 ~ 2025年8月
- 从早期「非遗传承」「创赛」转型到「学校不教社会化」系列
- 最高赞笔记：「学校不教社会化100：草率的率，也是效率的率」- 1.5万赞
- 爆款分析示例见文末

### B站相关内容采集注意

B站上针对非知名人物的视频**不要只看播放量**，还要检查是否真的是目标人物本人：
- B站搜索中文名可能出现大量同名/昵称相似的UP主，需要人工筛选
- 潘妙言在B站**几乎没有主动运营**，仅有的2个相关视频都是其他UP主的访谈节目
- 视频播放量极低（34和139播放），说明目标人群画像匹配度：B站 < 小红书
- 采集后应在报告中标明「各平台活跃度差异」

### 已知痛点

1. **小红书必须登录** — web_search 只返回摘要，web_fetch 只返回登录页。解决方案：上述"浏览器登录模式"
2. **搜索区域限制** — 百度/Google 对自动化访问友好度低，Bing 中文结果质量不高
3. **人物特殊性** — 非公众人物在国际上几乎无索引，纯中文内容，对中文搜索依赖度高
4. **知乎公开文章可读** — 意外发现 OpenClaw 浏览器可以直接渲染知乎文章全文
5. **小红书需要滚动加载** — 不滚动只看到10条笔记，滚动后最多可加载30-50条
6. **笔记详情页需逐个访问** — 无法批量抓正文，需要逐个 navigate + snapshot
7. **B站同名UP主干扰** — 需人工区分同名人，非目标人物的内容不应入报告
8. **各平台权重差异** — 目标人群可能只在 1-2 个平台活跃，其他平台的数据可忽略

### 优化要点（已纳入 SKILL.md）

1. ✅ 子 Agent 返回文本而非写文件
2. ✅ 搜索 API fallback 链
3. ✅ 浏览器登录模式操作流程
4. ✅ Chrome 148 兼容处理
5. ✅ 各平台可访问性对照表
6. ✅ 针对中国互联网环境的搜索策略
7. ✅ 小红书滚动加载采集策略
8. ✅ 小红书笔记详情页逐个提取
9. ✅ 爆款笔记六维分析框架
10. ✅ IP定位综合分析方法
11. ✅ 数据来源内联链接
12. ✅ 各平台活跃度权重评估
